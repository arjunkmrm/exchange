{
  "type": "module",
  "module": "./src/index.ts",
  "name": "@bitly/mcp",
  "version": "0.0.1",
  "description": "Bitly MCP (Model Context Protocol) Server Implementation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prebuild": "pnpm run --filter \"@bitly/sdk\" build",
    "build": "npx @smithery/cli build",
    "dev": "npx @smithery/cli dev",
    "prestart": "pnpm build"
  },
  "dependencies": {
    "@bitly/sdk": "workspace:*",
    "ethers": "^5.7.2",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "ts-node": "^10.9.1"
  }
}